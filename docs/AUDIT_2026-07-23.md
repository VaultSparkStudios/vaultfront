<!-- generated-by: render-audit-md.mjs — DERIVED FROM AUDIT_2026-07-23.json; do not hand-edit -->

# Project Audit — VaultFront

> Source of truth: the JSON sidecar. Edit JSON, then re-render.

## Ranked Plan

| #   | Tier | Axis                                                      | Effort | Impact | Innov | Priority | Status | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | :--: | --------------------------------------------------------- | ------ | :----: | :---: | :------: | :----: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 63  |  🔥  | feature depth / core loop / ux / soul fidelity            | 3h     |   10   |   7   |   92.0   |   ✓    | **public-playlist-vaultfront-loop-contract** — Make every scheduled public playlist actually run the VaultFront loop. MapPlaylist returns FFA, team, special, and ranked public configs without vaultSitesEnabled or intelOperationsEnabled. Both default false, and GameRunner installs VaultFrontExecution only when either is true. Private and single-player paths explicitly enable them, proving the public omission is real. **Recipe:** Centralize a public VaultFront config policy, apply it to FFA/team/special/1v1 configurations, and add integration-grade contract tests proving each scheduled public config installs the VaultFront execution path without changing private-host opt-in semantics.                                                                                                                                                                                                                                                                                                        |
| 64  |  🔥  | gamification / retention / security / progression         | 8h     |   10   |   9   |   86.0   |   ✓    | **certified-season-contract-ledger** — Replace client-authoritative seasonal counters with a certified durable contract ledger. WinModal computes arbitrary deltas and POSTs them to an endpoint whose schema has no game identity, upper bounds, or replay protection. Worker adds those values to a process-local Map, allowing inflation and restart loss despite the project's certified progression posture. **Recipe:** Create a typed CertifiedSeasonContractStore with PostgreSQL durability and an honest process-local fallback; derive bounded contract metrics only from AuthoritativeMatchOutcome; key events by player, season, and game for idempotency; make the client GET-only; return durability and evidence metadata; add duplicate, restart, transaction, and authorization tests.                                                                                                                                                                                                                   |
| 65  |  🔥  | feedback loop / observability / engagement / analytics    | 8h     |   9    |   9   |   78.0   |   ✓    | **certified-loop-funnel-evidence** — Measure the vault-to-convoy loop from certified game transitions instead of browser timestamps. WinModal reconstructs early/mid/late phases from an unbound sessionStorage stream and Date.now minus match length. The stream is not game-scoped or cleared and counts UI commands rather than first-vault latency, convoy completion, interception, repeat cycles, or abandonment. **Recipe:** Carry GameServer's authoritative intent funnel plus certified VaultFront result counters into a game-bound LoopEvidence receipt, persist one idempotent record per game and actor where available, derive first-cycle and repeat-cycle metrics without client claims, expose a privacy-minimal summary, retire the client funnel POST, and test cross-match isolation and duplicate completion.                                                                                                                                                                                       |
| 66  |  🔥  | observability / release / security                        | 3h     |   10   |   7   |   74.0   |   ✓    | **runtime-health-observation-truth** — Separate the static health-route contract from a live runtime health observation. generate-release-evidence regex-matches source for app.get('/_health'), stamps a fresh observedAt, and records status pass under healthEndpoint. That proves declaration, not reachability or a healthy deployment. **Recipe:** Rename the static evidence dimension to healthRouteContract, model live healthObservation as a provenance-bearing external receipt, keep runtimeAdvertised false without it, update manifest and readiness consumers, and add an adversarial test showing a declared-but-unobserved or 503 endpoint cannot become release-ready.                                                                                                                                                                                                                                                                                                                                  |
| 67  |  ⚡  | speed / organization / security / testability             | 6h     |   8    |   7   |   58.0   |   ✓    | **bounded-worker-router-composition** — Turn Worker.ts into a composition root with bounded domain routers. Worker.ts is roughly 4,000 physical lines and mixes identity, policy, readiness, experiments, AI, game lifecycle, clans, tournaments, progression, replay, and telemetry. This makes every route change a high-blast-radius edit and leaves many behaviors testable only through source scanning. **Recipe:** Extract the season-contract, loop-evidence, and spectator-prediction domains behind injected route dependencies; preserve RoutePolicy bindings and authentication at the seam; add direct router tests; and introduce a production-code composition budget ratchet so future domains cannot silently re-accumulate in Worker.ts.                                                                                                                                                                                                                                                                 |
| 68  |  ⚡  | new feature / spectator engagement / retention / security | 8h     |   8    |   8   |   52.0   |   ✓    | **durable-reachable-prediction-league** — Complete the Prediction League as a reachable, restart-durable spectator loop. The leaderboard UI is reachable, but no client calls prediction-league/predict; a separate narrator crowd route accepts votes without adding them to league history. PredictionLeagueStore calls itself persistent while storing pending and history exclusively in Maps, so restarts erase the league. **Recipe:** Unify crowd vote and league submission behind one authenticated prediction contract, persist pending predictions and resolved history with game/player uniqueness, resolve only from MatchProgression's certified outcome, add a compact spectator prediction surface with live aggregate feedback, and test replay, restart, privacy, and empty-state behavior.                                                                                                                                                                                                              |
| 69  |  🔥  | gamification / retention / security / progression         | 8h     |   10   |   9   |   86.0   |   ✓    | **certified-durable-season-pass** — Make the visible Season Pass a certified durable entitlement ledger. SeasonMilestoneStore is a process-local Map updated by the certified progression spine without game identifiers. Restarting erases progress and claims, replaying an outcome can double-count activity, and claim() only flips an in-memory boolean while the UI presents a title, badge, or gold-looking reward as earned. This is a second progression authority beside the durable certified contract ledger. **Recipe:** Replace activity-by-activity mutation with one certified game-bound Season Pass update per player; persist progress events, aggregate progress, and claims in PostgreSQL with honest process-local fallback; bind claims to the authenticated actor; represent rewards as durable title/badge entitlements rather than ungranted currency; expose durability/evidence metadata; extract the routes; and add restart, replay, authorization, and entitlement tests.                   |
| 70  |  🔥  | feedback loop / observability / game depth / automation   | 8h     |   9    |  10   |   81.0   |   ✓    | **release-bound-balance-envelope** — Turn VaultFront balance into a self-verifying release-bound envelope. GAMEPLAY_DESIGN declares itself authoritative, but the reward and cadence constants remain private fields spread through VaultFrontExecution and manually duplicated in prose. Property tests use Math.random and prove only a few relations. A plausible doc/code drift can therefore ship without a deterministic artifact showing fairness bounds, monotonicity, caps, mutator effects, or the exact tuning digest used by a release. **Recipe:** Extract the core convoy/cadence tuning into one typed frozen authority consumed by execution; expose a pure reward planner; generate a deterministic balance-envelope receipt across boundary and seeded scenario grids; fail on documented monotonicity, cap, comeback, and mutator invariants; bind the receipt digest into release evidence and public agent capability metadata; and replace random property checks with reproducible counterexamples. |
| 71  |  ⚡  | speed / organization / security / testability             | 6h     |   8    |   7   |   58.0   |   ✓    | **bounded-experiment-router-composition** — Extract the experiment control plane from Worker.ts behind one bounded router. Worker.ts is 4,027 lines against a 4,040-line ceiling and still embeds dock, recap, runtime, and unified experiment routes with repeated identity, integrity, admin, and summary logic. The prior router extraction created the seam but left this high-churn control plane one small edit away from breaking the composition budget. **Recipe:** Move all experiment assignment/event/summary/admin routes into an injected ExperimentRouter; preserve policy binding and actor/admin checks; test invalid variants, replayed event IDs, summaries, and unified results directly; teach route inventory and the composition ratchet about the extracted domain; and lower the Worker budget to bank the reduction.                                                                                                                                                                             |
| 72  |  🔥  | security / supply chain / automation                      | 1h     |   9    |   4   |   23.0   |   ✓    | **protobuf-denial-of-service-patch** — Patch the direct protobuf parser denial-of-service advisory and ratchet CI. Live npm audit reports direct dependency protobufjs 7.6.3 inside the affected 7.5.0–7.6.4 range for GHSA-j3f2-48v5-ccww. A non-major 7.6.5 fix is available, but CI runs npm audit at high severity and therefore permits this known moderate availability risk indefinitely. **Recipe:** Run Obelisk package-trust for protobufjs@7.6.5, update only the pinned dependency and lockfile, verify the production build and protocol imports, change CI to audit at moderate severity, and add a contract test that prevents the threshold from drifting upward.                                                                                                                                                                                                                                                                                                                                          |

## Depth Ladder

### public-playlist-vaultfront-loop-contract

- **L1** — Set both feature flags on public configs.
- **L2** — Centralize the policy and add MapPlaylist coverage for FFA, team, special, and 1v1.
- **L3** — Add a GameRunner reachability ratchet proving every scheduled public path installs VaultFrontExecution while private custom configuration remains explicit.

### certified-season-contract-ledger

- **L1** — Disable client mutation and update contracts from MatchProgression in memory.
- **L2** — Add typed certified match derivation, idempotency, read-only client refresh, and focused tests.
- **L3** — Add PostgreSQL event/progress tables, transaction-bound replay safety, explicit durability/evidence receipts, route extraction, and restart tests.

### certified-loop-funnel-evidence

- **L1** — Bind existing client telemetry to gameId and clear it at match boundaries.
- **L2** — Build a server-certified per-game receipt from GameServer intentFunnel and result counters, with a read-only summary.
- **L3** — Add durable idempotent storage, privacy-minimal cohort derivation, route extraction, and adversarial cross-match/replay tests.

### runtime-health-observation-truth

- **L1** — Rename the static field and copy to remove the false endpoint implication.
- **L2** — Add separate typed contract and observation dimensions plus generator tests.
- **L3** — Add signed/provenance-ready observation ingestion, readiness invariants, and declared-but-unreachable/503 adversarial fixtures.

### bounded-worker-router-composition

- **L1** — Extract one new domain router with injected dependencies.
- **L2** — Extract the three changed domains and add direct auth/error route tests.
- **L3** — Add shared composition types, policy-binding tests, a Worker size/domain ratchet, and source-scan replacement for extracted routes.

### durable-reachable-prediction-league

- **L1** — Wire an authenticated client prediction action to the existing league route.
- **L2** — Unify vote/league routes, add idempotent memory behavior, live aggregate UI, and router tests.
- **L3** — Add PostgreSQL persistence, certified resolution receipts, restart/replay tests, and privacy-minimal leaderboard identities.

### certified-durable-season-pass

- **L1** — Key in-memory activity by game and stop presenting ungranted rewards.
- **L2** — Create a typed certified store, actor-bound routes, idempotent game updates, and direct route/store tests.
- **L3** — Add PostgreSQL event/progress/claim transactions, restart-safe entitlements, explicit durability receipts, route extraction, and adversarial replay/auth tests.

### release-bound-balance-envelope

- **L1** — Export core tuning constants and assert a few documented values.
- **L2** — Create a shared pure reward planner and deterministic grid tests with counterexample output.
- **L3** — Generate a digestible balance envelope, self-verify it, attach it to release lineage and dual-audience surfaces, and ratchet the build against drift.

### bounded-experiment-router-composition

- **L1** — Extract the three event POST routes.
- **L2** — Extract all assignment/event/summary routes with injected dependencies and focused tests.
- **L3** — Extract the unified admin surface, add policy/integrity adversarial tests, scan the router in inventory, and ratchet Worker below its new measured size.

### protobuf-denial-of-service-patch

- **L1** — Apply the safe patch version and regenerate the lockfile.
- **L2** — Verify build/tests and npm audit returns zero known vulnerabilities.
- **L3** — Package-trust the patch, ratchet CI at moderate severity, and add a workflow-contract test.

## Execution Log

### public-playlist-vaultfront-loop-contract

- `function at() { [native code] }` · **noted** ·

### certified-season-contract-ledger

- `function at() { [native code] }` · **noted** ·

### certified-loop-funnel-evidence

- `function at() { [native code] }` · **noted** ·

### runtime-health-observation-truth

- `function at() { [native code] }` · **noted** ·

### bounded-worker-router-composition

- `function at() { [native code] }` · **noted** ·

### durable-reachable-prediction-league

- `function at() { [native code] }` · **noted** ·

### certified-durable-season-pass

- `function at() { [native code] }` · **noted** ·
- `function at() { [native code] }` · **noted** ·
- `function at() { [native code] }` · **noted** ·

### release-bound-balance-envelope

- `function at() { [native code] }` · **noted** ·
- `function at() { [native code] }` · **noted** ·
- `function at() { [native code] }` · **noted** ·

### bounded-experiment-router-composition

- `function at() { [native code] }` · **noted** ·
- `function at() { [native code] }` · **noted** ·
- `function at() { [native code] }` · **noted** ·

### protobuf-denial-of-service-patch

- `function at() { [native code] }` · **noted** ·
