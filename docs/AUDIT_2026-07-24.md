<!-- generated-by: render-audit-md.mjs — DERIVED FROM AUDIT_2026-07-24.json; do not hand-edit -->

# Project Audit — VaultFront

> Source of truth: the JSON sidecar. Edit JSON, then re-render.

## Ranked Plan

| #   | Tier | Axis                                                 | Effort | Impact | Innov | Priority | Status | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | :--: | ---------------------------------------------------- | ------ | :----: | :---: | :------: | :----: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 73  |  🔥  | gamification / retention / security / infrastructure | 8h     |   10   |   9   |   96.0   |   ✓    | **replay-safe-progression-fanout** — Turn certified progression into a replay-safe recoverable fan-out. MatchProgression inserts gameId into processedGameIds before its first asynchronous leg. A thrown leg leaves the ID claimed and every retry returns duplicate, while PlayerStatsStore can increment Elo and match counters again after restart because match_history has no (persistent_id, game_id) uniqueness. The visible progression stack is individually durable but its orchestration is not recoverable. **Recipe:** Make PlayerStatsStore atomically idempotent per game across memory and PostgreSQL, add the schema uniqueness invariant, replace pre-claim suppression with explicit in-flight/completed state that releases failed claims, and add concurrency, partial-failure, and fresh-spine replay tests proving each player/game changes rating once while failed fan-out can resume. |
| 74  |  🔥  | security / ux / organization                         | 5h     |   9    |   7   |   84.0   |   ✓    | **actor-bound-achievement-profile** — Make achievement progression an actor-bound profile contract. GET /api/vaultfront/achievements/:persistentId and the meta-chain variant accept any caller-supplied persistent ID and expose progress without a play token. Newer season and contract reads authenticate the actor and reject cross-player claims. Achievement progress is private player progression and should share that boundary. **Recipe:** Extract an AchievementRouter with injected authentication, bind optional path claims to the verified actor, update the client to send VaultFront identity headers, retain a compatibility path that fails closed on mismatch, add direct router tests, and include the router in the Worker composition budget.                                                                                                                                           |
| 75  |  ⚡  | feedback loop / observability / release truth        | 4h     |   9    |   8   |   88.0   |   ✓    | **self-validating-state-scope-ledger** — Make persistence readiness derive from executable store capabilities. StateScopeLedger declares playtest-pulse as process-only and release-critical even though Worker writes authenticated events through PlaytestEvidenceStore, which uses PostgreSQL when configured and labels process-local fallback honestly. Readiness can therefore emit a false volatility warning from stale handwritten metadata. **Recipe:** Correct the playtest evidence owner/scope, represent store capability separately from effective runtime scope, self-validate declared database-capable owners against an executable capability catalog, and add tests proving ready/absent/failed database postures produce truthful pass/warn/block outcomes.                                                                                                                                 |
| 76  |  ⚡  | feature depth / game loop / speed / testability      | 6h     |   8    |   8   |   80.0   |   ✓    | **vault-pressure-kernel** — Extract the Vault Pressure climax into a deterministic domain kernel. VaultFrontExecution is roughly 2,900 lines and directly owns the three-delivery breach threshold, 90-second expiry, win trigger, and fallback-to-two behavior inside mutable maps. That flagship climax has no pure transition contract or file-size ratchet, making balance-preserving changes harder to prove than the newer reward planner. **Recipe:** Extract a pure VaultPressureKernel that receives prior state plus certified transition input and emits next state/events, preserve the current three-delivery and expiry semantics exactly, adapt VaultFrontExecution as composition only, add exhaustive transition/property tests, and add a source composition ratchet that forbids the extracted state-machine body from returning.                                                             |

## Depth Ladder

### replay-safe-progression-fanout

- **L1** — Release processedGameIds on failure.
- **L2** — Add store-level player/game idempotency and failure-retry tests.
- **L3** — Add PostgreSQL advisory serialization plus unique schema protection, concurrent same-process promise coalescing, restart-safe downstream replay, and a canonical completion receipt digest.

### actor-bound-achievement-profile

- **L1** — Require authentication on the two reads.
- **L2** — Bind the path claim, update the client, and add authorization tests.
- **L3** — Extract an injected router, test missing/cross-player/valid reads directly, and ratchet Worker composition so the trust boundary cannot drift back.

### self-validating-state-scope-ledger

- **L1** — Correct the stale playtest-pulse ledger row.
- **L2** — Test the row against all database postures.
- **L3** — Add an executable store-capability catalog and a ledger integrity assertion so owner/scope drift fails tests instead of lying in readiness.

### vault-pressure-kernel

- **L1** — Extract pressure arithmetic into pure helpers.
- **L2** — Model delivery/expiry transitions and cover threshold edge cases.
- **L3** — Emit typed domain events, exhaustively verify deterministic transition sequences, integrate without balance changes, and ratchet the composition boundary.

## Execution Log

### replay-safe-progression-fanout

- `function at() { [native code] }` · **noted** ·

### actor-bound-achievement-profile

- `function at() { [native code] }` · **noted** ·

### self-validating-state-scope-ledger

- `function at() { [native code] }` · **noted** ·

### vault-pressure-kernel

- `function at() { [native code] }` · **noted** ·
