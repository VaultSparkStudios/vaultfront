<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-17 -->
<!-- project: vaultfront · type: game · session: 3 -->

# Project Audit — VaultFront (Session 3)

> One ranked improvement plan covering 9 axes. All session-1 (19 items) and session-2 (24 items) shipped. This is the session-3 plan.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 19
- Combined Priority: 383.8
- Top 3: `dynasty-story-engine` · `living-match-narrator` · `in-game-micro-coach`
- SIL baseline: 920/1000 · target: 960+

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
| --- | :--: | ------------ | ------ | :----: | :----: | :------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------- |
| 1 | 🔥 | ai | 2h | 8 | 8 | **32.0** | **dynasty-story-engine** — AI generates a 3-sentence clan legend after each match that accumulates across games. Every ghost route, heist, and betrayal becomes part of your dynasty's story. Players share it; clans become characters. **Recipe:** POST `/api/vaultfront/dynasty-story`: pass clan slug + last 5 match outcomes + top moments → Haiku prompt → append to `clan.dynasty_story` in DB → render in ClanModal below dynasty emblem. Use prompt caching on system prompt. |
| 2 | 🔥 | ai | 4h | 9 | 9 | **31.3** | **living-match-narrator** — Real-time AI broadcast commentary feed streamed to spectators. No game in this genre has a live AI narrator. SpectatorBus already exists; add NarratorBus that subscribes to key GameUpdateType events and streams 1-sentence calls (convoy intercept, last-stand, heist) via SSE. Players opt-in to TTS. **Recipe:** Subscribe to GameUpdateType events in Worker, batch 3 events → POST `/api/vaultfront/narrator` (streaming Haiku). Pipe SSE to SpectatorBus. Rate-limit: 1 call/15s per game. |
| 3 | 🔥 | ai + ux | 4h | 8 | 8 | **24.7** | **in-game-micro-coach** — Subtle AI action hint surfaced once at the 2-minute mark if the player hasn't issued a vault command. Non-intrusive: 8px text, auto-dismiss 12s, bottom-right corner. Opt-in via UserSettings. Dramatically improves new-player retention. **Recipe:** Add `CoachHintEngine.ts`: subscribe to UIStateManager state, fire at tick 1200 → GET `/api/vaultfront/micro-hint?gold=X&sites=Y` → Haiku 1-sentence suggestion → render in HUD overlay. Dedup: 1 hint per 3 minutes. |
| 4 | 🔥 | ux | 2h | 8 | 6 | **24.0** | **cinematic-intercept-moment** — Dramatic PixiJS gold-particle burst + HUD banner + SFX when a convoy is intercepted. The bounty-board and intercept mechanics are VaultFront's most exciting moments but are visually underwhelming. This transforms mechanical into visceral. **Recipe:** Add `InterceptCelebration.ts`: subscribe to `GameUpdateType.CONVOY_INTERCEPTED` → spawn ParticleEmitter (gold/yellow, 0.8s) at convoy tile + UIStateManager.showBanner('INTERCEPTED — +X GOLD') + howler SFX. |
| 5 | 🔥 | gamification | 8h | 8 | 9 | **21.7** | **fog-of-war-memory-decay** — Tiles unseen for 60s visually fade to last-known state at 50% opacity + grayscale. Revolutionary information-warfare mechanic no browser RTS does. Ghost convoys become even more powerful when opponents can't tell if a route is current. Creates an entirely new strategic layer. **Recipe:** Add `tileMemoryAge: Map<TileRef, number>` to TerritoryLayer. On render tick: if tile not in viewport, increment age; age > 600 ticks → PixiJS ColorMatrixFilter desaturate + 50% alpha. Hover tooltip: 'LAST SEEN Xs AGO'. Opt-in via UserSettings. |
| 6 | 🔥 | gamification | 4h | 7 | 8 | **21.7** | **route-intelligence-network** — After intercepting a convoy, sell the route to an ally for gold. They receive a 10-tick advance warning on future convoys from that source; you earn 15% of their potential reward. Formalizes intelligence as VaultFront's core strategic currency. **Recipe:** Add `VaultFrontCommand.SELL_INTEL`: check alliance + last intercept within 300 ticks → grant `intelActiveUntilTick` to ally, emit `INTEL_SOLD` update. Add `SellIntelButton` to HUD post-intercept for 30s. |
| 7 | 🔥 | featureDepth | 4h | 8 | 7 | **21.7** | **ignis-founder-feedback-loop** — Feed founder accept/reject/pivot signals into IGNIS ranking scores. IGNIS genius hit #1 from startup brief. Closes the human-in-the-loop gap identified as IGNIS's weakest pillar (CREATIVITY 0.20 signal gap). **Recipe:** Add POST `/api/ignis/signal`: `{itemSlug, signal: 'accept'                                                                                                                                                                                                                                                           | 'reject' | 'pivot', sessionId}`→ write to`ignis_signals` table → IGNIS scoring reads weighted average per pillar. Wire WinModal 'was this useful?' thumbs to this endpoint. |
| 8 | ⚡ | ai | 2h | 6 | 7 | **21.0** | **bot-ai-persona-backstories** — At game start, Haiku generates a unique militant codename + 2-sentence origin story for each bot (cached by personality+seed). Makes bots feel like rival commanders, not AI filler. Displayed in lobby and spectator leaderboard. **Recipe:** Extend `BotSpawner.ts`: on bot spawn, call GET `/api/vaultfront/bot-persona?personality=X&seed=Y` (24h memory cache). Worker adds endpoint: 20-token Haiku call → e.g. 'SHADOW MERCHANT — bribery-trained ghost-route economist'. Render in `LobbyPlayerView.ts`. |
| 9 | 🔥 | gamification | 8h | 8 | 8 | **19.3** | **spectator-economy** — Spectators wager spectator-gold on match outcomes. An odds board aggregates predictions in real-time. Top weekly predictor earns a cosmetic badge. Turns passive spectating into an active metagame nobody in this genre has built. **Recipe:** Add `SpectatorBet` table. POST `/api/spectator/bet` endpoint. SpectatorBus broadcasts bet aggregates every 30s. WinModal resolves bets → `PlayerStatsStore.addSpectatorGold()`. `LeaderboardTabs` adds SpectatorLeaderboard tab. |
| 10 | 🔥 | gamification | 8h | 8 | 8 | **19.3** | **world-events-system** — Server-orchestrated global events affecting ALL active matches for 10 minutes (e.g. 'GOLD SURGE: convoys +50% reward' or 'BLACKOUT: ghost route cooldown halved'). Creates shared community 'where were you' moments. **Recipe:** Add `WorldEventScheduler.ts`: cron 2×/day, picks event from `WorldEventConfig`, broadcasts via GameManager to all Workers → `VaultFrontExecution.applyWorldEvent(type, durationTicks)`. HUD banner + Discord announce + `world_events` table. |
| 11 | ⚡ | ai | 4h | 7 | 7 | **18.9** | **predictive-pre-match-oracle** — AI shows each player's predicted ELO change + 'your biggest threat' before the match starts. Pre-match tension is the most underutilized engagement window. Creates stakes before a single tick fires. **Recipe:** Extend `GameStartingModal.ts`: on game-start, POST `/api/vaultfront/match-oracle?players=[ids]` → Haiku prompt with ELO history → render prediction card. Expire after 30s. |
| 12 | ⚡ | featureDepth | 4h | 7 | 7 | **18.9** | **streaming-overlay-api** — Public WebSocket endpoint emitting sanitized match events for OBS browser source overlays. Community streamers become your best marketing channel. **Recipe:** Add `StreamingBus.ts`: subscribe to SpectatorBus events, strip PII (stable-hash playerIds), emit JSON to `/stream/:gameId`. Add `StreamOverlayPage.ts` Lit component at `/overlay/:gameId` — styled HUD for browser-source embedding. Document in README. |
| 13 | ⚡ | ux | 2h | 7 | 5 | **17.5** | **in-game-mutator-vote** — Move weekly mutator vote from Discord-only to WinModal in-game. 10× more votes, better signal quality. **Recipe:** WinModal already calls `castMutatorVote()` and `fetchMutatorVoteStatus()`. Add `renderMutatorVoteCard()` showing top 2 candidates with live counts + vote buttons. Add vote-confirmation animation (green pulse). Discord announce still fires. |
| 14 | ⚡ | ux | 2h | 7 | 5 | **17.5** | **season-contract-hud** — In-game HUD widget (top-right, 3 progress bars) showing live contract progress during play. Drives directed behavior and session length. **Recipe:** Add `ContractHudWidget.ts` as Lit component. Mount in `ClientGameRunner` after game-start. Fetch from `/api/vaultfront/contracts` (already exists) → render 3 most-in-progress contracts. Update on `VaultFrontStatusUpdate`. Collapse to icon on mobile. |
| 15 | ⚡ | security | 2h | 7 | 5 | **17.5** | **automated-anti-cheat-alerts** — Discord/email alert when anti-cheat signals exceed threshold. Converts reactive moderation to proactive. **Recipe:** Add `AntiCheatMonitor.ts`: setInterval 30min → `PlayerStatsStore.getFlaggedMatches(last30min)`, if count > threshold → POST to `DISCORD_WEBHOOK_URL` via existing `DiscordNotifier` with flagged list. Add `ANTI_CHEAT_ALERT_THRESHOLD` env var (default: 3). |
| 16 | ⚡ | ux | 4h | 7 | 6 | **16.3** | **match-replay-highlight-reel** — Auto-string top 3 ReplayMoments into an auto-play highlight sequence. ReplayHighlightStore exists. The 30s highlight reel is the natural shareable unit for social. **Recipe:** Add `ReplayReelBuilder.ts`: fetch top 3 moments by global engagement → `ReplayPanel` reel mode with auto-advance + 1.5s transitions. Add 'Watch Highlights' in WinModal. Add `shareReplayReel()` to `Api.ts`. |
| 17 | 💡 | tokenCost | 2h | 6 | 5 | **15.0** | **narrative-batch-caching** — Consolidate 5 per-match AI narrative calls into 1 batched Haiku request with prefix caching. Current: 5 separate calls with identical system prompts. Target: 1 call ~60% cheaper. Measurement: context-meter ledger before/after. **Recipe:** Add `NarrativeBatchWorker.ts`: on game end, single Haiku request with `prompt-caching-2024-07-31`, return JSON with keys `{narrative, prophecy, commentary, mission, lore}`. Split to existing handlers. |
| 18 | ⚡ | gamification | 8h | 8 | 6 | **14.4** | **clan-war-tournament** — Auto-seeded weekly elimination bracket for top-16 ELO clans. Clan ELO exists but has no competitive destination. Winner earns Discord role + cosmetic. **Recipe:** Extend `TournamentStore.ts` with clan bracket logic from `ClanStore.getLeaderboard(limit:16)`. POST `/api/clan-war/seed` (cron Monday midnight). Add `ClanWarBracketPage.ts` (bracket visualization). Auto-announce via `DiscordNotifier`. |
| 19 | 💡 | speed | 4h | 6 | 5 | **11.6** | **ab-results-dashboard** — Admin page surfacing dock/recap/runtime AB test results. AB infra is write-only without this. **Recipe:** Add GET `/api/admin/ab/results`: aggregate events by variant → Wilson score CI. Add `AbDashboard.ts` Lit page at `/admin/ab` behind `Privilege.ADMIN`. Table: variant | events | conversions | rate | 95% CI. |

## Innovation Reserve (Innovation ≥ 8 — execute even when busy)

- `living-match-narrator` (Innovation 9) — live AI broadcast commentary
- `fog-of-war-memory-decay` (Innovation 9) — decaying tile memory, revolutionary mechanic
- `dynasty-story-engine` (Innovation 8) — accumulating AI clan legend
- `spectator-economy` (Innovation 8) — spectators bet on outcomes
- `in-game-micro-coach` (Innovation 8) — subtle mid-match AI hints
- `route-intelligence-network` (Innovation 8) — sell intercepted intel to allies
- `world-events-system` (Innovation 8) — server-orchestrated global match events

## Skipped (Priority < 8 or covered by tooling)

- `worker-route-modularization` (Priority 6.96) — organizational refactor, no player impact; defer to debt sweep
- `csp-headers-audit` — Helmet.js already in deps; one-liner config change, not an audit item

---

_Generated by `/audit` v1.0 · Session 3 · SIL baseline 920 · ready for `/implement`_
