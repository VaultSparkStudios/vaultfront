<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-18 -->
<!-- project: vaultfront · type: game -->

# Project Audit — VaultFront

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 18 new + 6 previously shipped
- Combined Priority (new): 1026.2
- Top 3: `spectator-crowd-prediction` · `vault-intelligence-market` · `coach-hint-event-triggers`

## Ranked Plan

| #   | Tier | Axis                   | Effort | Impact | Innov. | Priority | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | :--: | ---------------------- | ------ | :----: | :----: | :------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   |  🔥  | gamification           | 2h     |   8    |   8    | **96.0** | **spectator-crowd-prediction** — Spectators watching via NarratorBus SSE submit live predictions (intercept / delivery-completes); a crowd consensus bar updates in the overlay; top-correct predictors earn cosmetic commendations. **Recipe:** Add POST `/api/vaultfront/narrator/:gameId/predict`. Store per-game prediction maps in memory. Broadcast updated tally as SSE `crowd_vote` event. Render intercept% vs delivery% bar in StreamOverlayPage. Reset on next convoy launch.                                               |
| 2   |  🔥  | gamification + feature | 4h     |   9    |   9    | **94.0** | **vault-intelligence-market** — Before launching a convoy, players spend gold to buy route-risk intelligence from allies who have scouted that corridor. Creates an in-match information economy — nobody in the RTS genre has this. **Recipe:** Add `VaultIntelCommand ('purchase_route_intel')` in VaultFrontExecution: deduct buyer gold, credit 10% to seller, return routeRisk + interceptProbability as a buyer-only activity event. Add buy_intel overlay in VaultFrontLayer. Expose via POST `/api/vaultfront/intel-purchase`. |
| 3   |  🔥  | ai + gamification      | 1h     |   7    |   6    | **79.5** | **coach-hint-event-triggers** — CoachHintEngine fires exactly once per game (`hasFetched=true`). Players losing a convoy, getting a bounty placed, or triggering a last-stand receive zero guidance. **Recipe:** Remove `hasFetched` guard; replace with per-trigger cooldowns (`Map<string, number>`). Add four trigger types: convoy_lost, bounty_placed_on_player, last_stand_nearby, chain_broken (each 5-min cooldown). Pass trigger type as additional param to `fetchMicroHint()`.                                              |
| 4   |  🔥  | ux + gamification      | 1h     |   6    |   7    | **79.5** | **play-style-mid-match** — Play-style card (Iron Fist / Convoy Lord / Shadow Broker) only appears post-game. Showing emerging style mid-match (after 60 ticks) lets players adapt before outcomes are locked. **Recipe:** Extract `WinModal.computePlayStyle()` into a `PlayStyleClassifier` util. In `VaultFrontLayer.tick()`, after 60 ticks, compute style from local activity counts and render a sidebar chip ('Emerging: Convoy Lord ⚡') in GameRightSidebar, updating every 120 ticks. Pure client-side — no API call.         |
| 5   |  🔥  | gamification           | 1h     |   8    |   5    | **75.7** | **elo-winmodal-animation** — WinModal shows new rank (RankBadge) but no animated Elo delta. The moment of gaining/losing points is the most emotionally potent frame in a competitive game — currently silent. **Recipe:** Add `eloOld` + `eloNew` to post-game data. Animate counter old → new over 1.5s via `requestAnimationFrame`. Color green/red. Show delta badge (+32, -18). On tier-boundary cross, fire full-screen shimmer CSS keyframe. Extend `/api/vaultfront/outcome/summary` to return `previousElo` + `currentElo`.   |
| 6   |  🔥  | gamification           | 4h     |   9    |   7    | **73.1** | **daily-challenge-system** — No daily objective system exists. Daily challenges are the highest-ROI retention lever in competitive games. **Recipe:** Add `DailyChallengeStore`: UTC-midnight seeded selection from preset pool (intercept 3 convoys / control 5 sites / ghost route / last-stand). Expose GET `/api/vaultfront/daily-challenge`. Wire progress via VaultFrontExecution activity events. Render compact card in GameRightSidebar. Award bonus gold on completion.                                                      |
| 7   |  🔥  | ai + ux                | 30m    |   7    |   9    | **71.5** | **narrator-sentiment-persona** — NarratorBus uses a single fixed commentary voice. Spectator-selectable HYPE / TACTICAL / COMEDIC personas make watching expressive — nobody in RTS streaming offers per-viewer commentary style. Token-cost neutral. **Recipe:** Add `persona` query param to `/api/vaultfront/narrator/:gameId`. Store persona per client in NarratorBus. In `flush()`, select system prompt variant by persona. Broadcast persona-specific commentary only to matching clients. Default: hype.                      |
| 8   |  ⚡  | gamification           | 1h     |   7    |   5    | **66.2** | **seasonal-rank-decay** — Players inactive 7+ days see their rank badge marked 'Decaying'. Creates daily retention pressure (visual indicator only — no stat change). **Recipe:** Add `lastMatchAt` to PlayerStatsStore. Compute `isDecaying = (now - lastMatchAt) > 7d` in leaderboard + contracts endpoints. In RankBadge, render pulsing orange border + 'Decaying' tooltip. Add 'Play to stop decay' CTA in GameStartingModal.                                                                                                     |
| 9   |  ⚡  | engagement             | 45m    |   5    |   6    | **61.7** | **mutator-live-vote-banner** — Community mutator vote happens on Discord; in-match players are unaware. Surfacing live vote standings during the final 5 minutes drives community engagement. **Recipe:** Include `voteStandings` in `VaultSeasonScheduler.getStatus()`. In VaultFrontLayer, when ticks > totalTicks − 300, fetch `/api/season/current` once and render a compact vote-standings card in GameRightSidebar with 'Vote on Discord' link.                                                                                 |
| 10  |  ⚡  | ai + feature-depth     | 1h     |   6    |   6    | **56.8** | **dynasty-story-winmodal** — Dynasty story endpoint exists + generates a 300-word clan legend, but WinModal only shows the play-style card. Clan players deserve to see their narrative evolve after each match. **Recipe:** In WinModal post-game fetch, if player has `clanId`, call `fetchDynastyStory(clanId)`. Render below play-style card as 'Clan Legacy' section with typewriter animation (10ms/char). Add 'Share to Discord' copy button.                                                                                   |
| 11  |  ⚡  | ux + gamification      | 1h     |   6    |   5    | **56.8** | **elo-rank-sparkline** — RankBadge shows current rank but no trend. A 10-match sparkline on hover communicates 'climbing' vs 'dropping' at a glance. **Recipe:** Store last 10 `eloHistory` entries in PlayerStatsStore. Add to `/api/vaultfront/contracts` response. In RankBadge, render SVG sparkline (10 points, 60×18px) on `:hover`. Color green if last > first, red otherwise. Add 'Climbing ▲' / 'Dropping ▼' label.                                                                                                          |
| 12  |  ⚡  | ai                     | 45m    |   8    |   6    | **49.3** | **narrator-match-context** — NarratorBus receives only an `activityLabel` with zero game context, producing generic commentary. **Recipe:** Add `NarratorContextSnapshot` {tickBucket, leadingPlayer, siteBalance, mutator}. Pass snapshot with every `queueEvent()` call in Worker.ts. In `flush()`, inject into user message. System prompt stays cached — only user message changes.                                                                                                                                                |
| 13  |  ⚡  | ux + engagement        | 2h     |   7    |   7    | **49.0** | **post-match-share-card** — Players have no artifact to share after a match. A canvas-generated image card gives organic social reach with no backend infra. **Recipe:** In WinModal, after Elo animation, show 'Share Result' button. Use `OffscreenCanvas` to draw: logo, outcome banner, rank delta, top stat, QR code (`qrcode.js`) for the replay URL. Convert to PNG → download or `navigator.clipboard.write()`.                                                                                                                |
| 14  |  ⚡  | ai + feature-depth     | 2h     |   7    |   8    | **42.0** | **replay-ai-highlight** — ReplayHighlightStore exists but highlight selection is manual. Scoring 30s windows by event density auto-selects the best clip — zero API cost. **Recipe:** In `ReplayHighlightStore.getOrCreate()`, score windows: intercept=10, last_stand=8, heist=7, bounty_collected=5, surge_entry=4. Set `autoHighlightTick` to max-score window. Add to `/api/replay/:gameId/highlight` manifest. In ReplayPanel, add '⚡ Best Moment' button seeking to `autoHighlightTick`.                                        |
| 15  |  ⚡  | ux                     | 1h     |   8    |   4    | **40.4** | **overlay-priority-queue** — VaultFrontLayer renders 7+ simultaneous overlay types with no coordination; late-game multi-event conflicts degrade readability. **Recipe:** Define `OverlaySlot` priority enum (LAST_STAND=1 highest, HEIST, SURGE, BOUNTY, WARCHEST, WORLD_EVENT, COMBO, MUTATOR). In `tick()`, render only the highest-priority active overlay in the primary slot; queue others in `activeOverlayQueue: Array<{type, expiresAtTick}>`.                                                                                |
| 16  |  💡  | tokenCost              | 30m    |   6    |   4    | **18.1** | **token-oracle-cache** — Every game-start generates a separate Haiku call for oracle + prophecy. Same mapName + playerCount tier = identical results within 5 minutes. **Recipe:** Add `Map<string, {data, expiresAt}>` LRU cache (max 50 entries, 5-min TTL) in Worker.ts. Key = `${mapName}:${playerCountBucket}`. Check cache before each Anthropic call; store on miss. Add hit-counter to admin debug endpoint.                                                                                                                   |
| 17  |  💡  | speed + organization   | 30m    |   8    |   2    | **12.1** | **master-ts-build-fix** — `src/server/Master.ts(166,30)` type error blocks `npm run build-prod` every session. **Recipe:** Read Master.ts line 166. Apply minimal correct type fix. Run `tsc --noEmit` to verify clean. Run `npm run build-prod` to confirm build completes.                                                                                                                                                                                                                                                           |
| 18  |  💡  | speed + organization   | 30m    |   6    |   1    | **4.5**  | **global-lint-unblock** — `npm run lint` fails on e2e/project-service config and Studio script issues, masking new regressions. **Recipe:** Run `npm run lint`. For e2e config issues, add targeted eslint ignores or fix imports. Add failing Studio scripts to `.eslintignore`. Verify clean pass.                                                                                                                                                                                                                                   |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- **spectator-crowd-prediction** (Innov: 8) — live crowd prediction SSE layer; nobody does this in RTS
- **vault-intelligence-market** (Innov: 9) — real-time in-match intelligence economy; genuinely novel
- **narrator-sentiment-persona** (Innov: 9) — per-spectator commentary tone; unique in genre
- **replay-ai-highlight** (Innov: 8) — heuristic-scored auto-highlight; zero API cost

## Skipped (Priority < 1.5)

- 0 items deferred.

---

_Generated by `/audit` v1.0 · ready for `/implement`_

## Previously Shipped (2026-05-18 Repair Pass)

- ✅ `startup-brief-repair` — Added `scripts/lib/cross-repo-tasks.mjs`, `ignis-insight.mjs`, `human-action-ages.mjs`; startup brief render now completes.
- ✅ `contract-hud-live-progress` — `ContractHudWidget` processes live `VaultFrontActivity` updates every tick; regression test added.
- ✅ `coach-hint-site-signal` — `CoachHintEngine` tracks `VaultFrontStatus`, counts local vault sites, avoids stale proxy; regression test added.
- ✅ `stream-overlay-reconnect-memory` — `StreamingBus` keeps bounded recent-event buffer, replays to reconnecting overlays, unrefs timers.
- ✅ `narrator-bus-token-guardrails` — `NarratorBus` lazy-loads Anthropic, deduplicates labels, caps events, sanitizes commentary, replays last call to late subscribers.
- ✅ `anti-cheat-alert-cooldown` — `AntiCheatMonitor` applies alert cooldown, bounds retained game IDs, exposes test/debug state.
