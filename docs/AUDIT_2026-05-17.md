<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-17 -->
<!-- project: vaultfront · type: game -->

# Project Audit — VaultFront

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 19
- Combined Priority: 757.9
- Top 3: `surge-chronicle` · `last-stand-event` · `convoy-intercept-predictor`

## Axis weights applied (game type)

| Axis          | Weight | Rationale                             |
| ------------- | :----: | ------------------------------------- |
| Gamification  |   3×   | Core RTS differentiator               |
| UX            |   2×   | Browser RTS retention hinge           |
| AI            |  1.5×  | Bot quality + intelligence features   |
| Feature depth |  1.5×  | Deepening VaultFront-specific systems |
| All others    |   1×   | Standard                              |

---

## Ranked Plan

| #   | Tier | Axis          | Effort | Impact | Innov. | Priority | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | :--: | ------------- | ------ | :----: | :----: | :------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   |  🔥  | gamification  | 4h     |   9    |   8    | **83.6** | **surge-chronicle** — Cinematic comeback overlay when surge activates. The surge mechanic exists but is invisible — no visual, no audio, no signal to opponents. This is a narrative turning point that nobody can see. **Recipe:** In `VaultFrontLayer.ts`, subscribe to `VaultFrontStatusUpdate.surgeState`; on surge entry apply CSS keyframe animation on surging player's territory tiles + render `SurgeChronicleOverlay` with countdown timer + emit audio via existing Fx system.                                            |
| 2   |  🔥  | gamification  | 8h     |   9    |   10   | **81.3** | **last-stand-event** — Server-wide event when one player controls 5+ vault sites. Creates emergent kingmaker dynamics; opponents receive +40% convoy gold for 90s. Breaks dominant-player stagnation and makes every endgame politically volatile. **Recipe:** In `tickVaultSites()`, count sites per player; on 5-site threshold fire `GameUpdateType.LAST_STAND_ACTIVATED`; broadcast full-width alert banner in `VaultFrontLayer.ts` + apply opponent gold multiplier for 90s.                                                    |
| 3   |  🔥  | gamification  | 8h     |   8    |   9    | **65.1** | **convoy-intercept-predictor** — Real-time 0–100% interception-probability gauge on active convoys. Rich risk math already exists (`routeRisk`, `rewardMultiplier`) but players see none of it. Transforms convoy routing from blind bet into tactical calculation. **Recipe:** Add `computeInterceptRisk(convoy, game)` scoring corridor between source and destination tiles by enemy troop density within 15 tiles; expose via `VaultFrontStatusUpdate`; render thin gradient bar under each convoy icon in `VaultFrontLayer.ts`. |
| 4   |  🔥  | ai            | 4h     |   9    |   10   | **52.2** | **ai-battle-narrative** — Post-game match story generated by Claude Haiku. Pass the top 8 structured game events (vault captures, convoy deliveries, surge activations, execution chains) to Claude Haiku; render 3-paragraph epic battle chronicle in `WinModal.ts`. Shareable as text card. **Recipe:** Add `/api/battle-narrative` endpoint; call `claude-haiku-4-5-20251001` with event-log prompt; inject HTML fragment into `WinModal`. Estimated cost: ~800 tok/match ≈ $0.0003. Measure via context-meter ledger.            |
| 5   |  ⚡  | gamification  | 8h     |   7    |   8    | **50.6** | **mutator-vote** — Players vote on next week's mutator at game end. Drives weekly return visits and gives players agency. 3 options shown in `WinModal`; server tallies; top vote becomes `weeklyMutator` at Sunday reset. **Recipe:** Add `POST /api/mutator-vote`; `mutator_votes` DB table; extend `VaultSeasonScheduler.ts` to select winner; add `MutatorVoteButton` components to `WinModal.ts`.                                                                                                                               |
| 6   |  ⚡  | ux            | 4h     |   8    |   8    | **49.5** | **combo-meter** — Street Fighter-style combometer for execution chain. `executionChainStep` is a 3-step gold-multiplier combo with zero visual feedback — most players don't know it exists. Add 3 diamond icons to `GameLeftSidebar.ts` filling per step; flash 'CLEAN EXECUTION ×1.2' banner on completion. **Recipe:** Subscribe to `VaultFrontStatusUpdate.executionChainState` in `UILayer`; render 3 SVG diamonds; on `CHAIN_COMPLETE` trigger `HeadsUpMessage`; on break play shatter particle via `FxLayer`.                 |
| 7   |  ⚡  | gamification  | 4h     |   7    |   6    | **48.7** | **achievement-spotlight** — Trophy moment in post-game screen on first unlock. Currently only a small mid-game toast. Pause `WinModal` reveal 3s to show full-panel achievement card with trophy animation. This is the 'holy shit' moment players tell friends about. **Recipe:** Check `AchievementStore.checkAndUnlock()` result before score reveal in `WinModal.ts`; if new unlock, show `AchievementSpotlightPanel` for 3s (CSS animation) then auto-advance.                                                                  |
| 8   |  ⚡  | ux            | 2h     |   8    |   6    | **48.0** | **surge-visibility** — Pulsing amber territory tint when any player is in surge. 2-hour change to `TerritoryLayer.ts` — overlay semi-transparent amber fill using Canvas `globalAlpha` with sine-wave intensity factor keyed to `Date.now()`, looking up `surgeUntilTick` from `VaultFrontStatusUpdate`. No new data needed. **Recipe:** Read `surgeUntilTick` per player from existing status update; apply amber overlay in territory render loop with `alpha = 0.15 + 0.1 * sin(Date.now() / 400)`.                               |
| 9   |  ⚡  | gamification  | 8h     |   8    |   6    | **43.4** | **elo-placement-progression** — Placement matches + visual rank badge + seasonal decay. `EloRating.ts` is clean but there are no placement matches, no rank display, no decay. Add: 5-game placement phase (K=64), rank badge in `PlayerPanel.ts` and leaderboard, progress bar to next tier, seasonal soft-reset (−200 cap) at `VaultSeasonScheduler` rollover. **Recipe:** Extend `PlayerStatsStore.ts` with `matchCount`/`placementComplete`; add `RankBadge` component; extend `VaultSeasonScheduler.ts` for soft-reset.         |
| 10  |  ⚡  | feature_depth | 4h     |   8    |   7    | **32.5** | **replay-highlight-sharing** — Shareable 30-second game moment clips. `ReplayHighlightStore.ts` and `ReplayPanel.ts` exist but there is no share mechanism. Add 'Clip This' button packaging timestamp ±15s into a short URL with OG meta tags. Primary viral acquisition loop for browser RTS. **Recipe:** Add `ClipButton` to `ReplayPanel.ts`; `POST /api/replay/<id>/clip {startTick, endTick}`; `ReplayStore.ts` stores clip; `/clip/<clipId>` route serves OG tags via `GamePreviewBuilder.ts`. ~110 lines total.              |
| 11  |  ⚡  | ai            | 8h     |   8    |   8    | **28.9** | **smart-spectator-camera** — Auto-follow highest-action zone in spectator mode. Score each region every 120 ticks by `(troop_density × 0.4 + vault_activity × 0.4 + convoy_proximity × 0.2)`; smoothly pan to top-scoring zone; override on click. Makes streaming engaging without gameplay changes. **Recipe:** Add `SpectatorAutoCamera` class with decaying heatmap; in `ClientGameRunner.ts` spectator mode, call `gameRenderer.setViewCenter(autoCamera.topRegionCenter())` every 5s with lerp. Toggle via 'A' key.            |
| 12  |  ⚡  | feedback_loop | 2h     |   8    |   7    | **28.0** | **post-game-micro-feedback** — 3-emoji signal capture at game end. Trust is at 52% and proposals/sessions are flagged signal gaps. Add '🔥 Epic' / '⚖️ Balanced' / '😤 Off' buttons to `WinModal.ts`; single tap; no typing. Feed into mutator weighting. **Target:** feedback_rate > 30% (votes/games). **Recipe:** `MicroFeedbackBar` component in `WinModal.ts`; `POST /api/match-feedback`; `match_feedback` DB table; factor into `VaultSeasonScheduler` mutator selection. ~40 lines.                                          |
| 13  |  💡  | feature_depth | 1d     |   8    |   9    | **23.0** | **convoy-ghost-route** — Pay 50k gold to dispatch a decoy convoy visible to enemy intel but carrying no reward. Creates mind-game layer over convoy routing; forces opponents to choose between wasting troops on an intercept vs. ignoring a potential real convoy. **Recipe:** Add `ghostConvoy: boolean` to `VaultConvoy`; handle `GHOST_ROUTE` in `VaultConvoyCommandExecution.ts`; broadcast as real to opponents in `VaultFrontStatusUpdate`; reveal on arrival.                                                               |
| 14  |  💡  | feature_depth | 4h     |   7    |   5    | **20.3** | **mutator-variety** — 8 new weekly mutators: gold_rush, blitz, no_mercy, contested, shield_escort, fog_of_war, rally_point, execution_rush. Doubles rotational variety and pairs with mutator-vote to keep weekly experience fresh. **Recipe:** Extend `weeklyMutator` union type in `VaultFrontExecution.ts`; handle each in relevant tick/compute methods.                                                                                                                                                                         |
| 15  |  💡  | feature_depth | 2h     |   6    |   4    | **18.0** | **dynamic-vault-scaling** — Raise site cap to 8 for 8+ player maps + 'Vacant Vault' alert at 600 ticks uncontrolled (doubles capture reward for 5 min). Turns dead zones into late-game flashpoints. **Recipe:** Change `Math.min(5,...)` to `Math.min(8,...)` gated on `game.allPlayers().length >= 8`; track `uncontrolledSinceTick` per site; emit alert + 2× multiplier at threshold.                                                                                                                                            |
| 16  |  💡  | ai            | 1d     |   9    |   5    | **14.4** | **bot-vaultfront-awareness** — Bots currently delete all structures and ignore vault sites/convoys entirely. Solo play is effectively broken for VaultFront mechanics. Add vault-site targeting bias (+50 score on adjacent tiles) to `AiAttackBehavior.ts`, convoy dispatching in `BotExecution.tick()`, and gate `deleteAllStructures()` to not delete Defense Posts. **Recipe:** Modify `AiAttackBehavior.ts` neighbor scoring; add `maybeDispatchConvoy()` to `BotExecution`; remove blanket structure deletion.                 |
| 17  |  💡  | security      | 4h     |   7    |   3    | **8.1**  | **api-auth-security** — Validate VaultFrontCommand payloads server-side (issuer alive + ownership check) + 5/s rate limit per player. Required before ghost convoy ships to prevent gold-mining exploits. **Recipe:** Add guard clauses in `handleQueuedCommands()` in `VaultFrontExecution.ts`; add per-player command timestamp ring buffer; validate in `Server.ts` input handler.                                                                                                                                                |
| 18  |  💡  | token         | 4h     |   5    |   4    | **7.7**  | **context-token-ledger** — Wire context-meter ledger for accurate session cost tracking (currently heuristic-only). Measurement: `context-meter --json` should show `ledgerEntries > 0`. **Recipe:** Set `ANTHROPIC_API_KEY` in `.env.local`; verify ledger path writable; confirm `confidence` shifts from `'heuristic'` to `'ledger'` after one session.                                                                                                                                                                           |
| 19  |  💡  | speed         | 4h     |   6    |   2    | **4.6**  | **missing-infra-scripts** — Write stubs for 4 missing scripts breaking studio workflow: `lib/blocker-rules.mjs`, `lib/task-board.mjs`, `ops.mjs`, `check-brief-staleness.mjs`. **Recipe:** `blocker-rules.mjs` exports empty `RULES` array; `task-board.mjs` reads and parses `context/TASK_BOARD.md`; `ops.mjs` delegates `blocker-preflight` subcommand; `staleness` checks `STARTUP_BRIEF.md` mtime vs 24h.                                                                                                                       |

---

## Innovation reserve (Innovation ≥ 8 — execute even when busy)

- `surge-chronicle` (innov 8) — invisible mechanic made visible
- `last-stand-event` (innov 10) — dynamic political event, no RTS does this
- `convoy-intercept-predictor` (innov 9) — real-time tactical intelligence layer
- `ai-battle-narrative` (innov 10) — LLM match story, zero competitors have it
- `mutator-vote` (innov 8) — player-shaped weekly meta
- `combo-meter` (innov 8) — fighting-game feedback in an RTS
- `convoy-ghost-route` (innov 9) — deception mechanic, deeply original
- `smart-spectator-camera` (innov 8) — AI-directed spectating

## Skipped (Priority < 1.5)

- `vaultfrontexecution-decompose` — priority 2.6; god-class at 1804 lines but extracting ConvoyManager/VaultSiteManager/BeaconManager/SurgeManager is pure refactor with no user-visible impact. Defer to dedicated tech-debt session.

---

_Generated by `/audit` v1.0 · 2026-05-17 · ready for `/implement`_
