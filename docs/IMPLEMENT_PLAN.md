<!-- generated-by: /implement Session 74 -->

# Implementation Plan — Session 74

Source: `docs/AUDIT_2026-07-16.json` (pending items only). L2 is the default rung; every item requires behavior verification before `shipped`.

## Wave A — Security and runtime truth

1. `decision-grade-experiment-integrity` — extract trusted experiment ledger; server assignment, unit weight, authenticated actor, event-id dedupe, rejection counters.
2. `server-authoritative-mutation-boundary` — central verified actor gate across progression/clan/tournament/private-lobby mutations and clients.
3. `websocket-ingress-backpressure-budget` — payload, connection, spectator, and slow-consumer limits.
4. `remote-ai-reservation-truth` — validate/cache before reservation; correct per-process scope; counters/tests.
5. `worker-health-scope-watermark` — IPC/listener/game-loop watermarks, 503 behavior, process-local evidence truth.

## Wave B — Protocol and release truth

6. `truthful-project-doctor` — real probes, direct exit codes, blockingFailing JSON, failure fixture.
7. `audit-driven-genius-contract` — latest audit ingestion and one cache schema through closeout.
8. `release-cost-truth-reconciliation` — local contradiction gate plus signed Ark correction cargo.
9. `release-truth-pipeline` — headers, dead CTA/agent endpoint honesty, container public parity, promotion provenance.

## Wave C — Build and observability budgets

10. `warning-free-build-boundary` — metadata/JSON/chunk warning root fixes and ratchet.
11. `observability-cardinality-transfer-budget` — metric label allowlist plus entry/media transfer budgets.

## Expansion

After all 11 items verify, regenerate the Unified Genius List, run the innovation-pack generator, and implement every new second-order candidate before closeout.
