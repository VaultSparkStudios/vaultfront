# Session 83 Implementation Plan

Source: `docs/AUDIT_2026-07-24.json`. All items run at L3.

1. `replay-safe-progression-fanout` — establish replay-safe storage and orchestration before changing consumers.
2. `actor-bound-achievement-profile` — converge the remaining private progression read onto the verified actor boundary.
3. `self-validating-state-scope-ledger` — derive readiness from executable store capabilities after persistence behavior is settled.
4. `vault-pressure-kernel` — extract the flagship climax state machine behind a deterministic composition boundary.

Each item requires focused tests, TypeScript, and its executable contract check before it can be marked shipped. Full lint, production build, coverage, E2E, doctor, and work-exhaustion gates run after the primary and innovation waves.
