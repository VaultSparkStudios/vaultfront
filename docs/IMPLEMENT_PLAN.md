<!-- generated-by: /implement Session 75 -->

# Implementation Plan — Session 75

Source: `docs/AUDIT_2026-07-16.json` (14 premise-verified pending items). L2 is the default rung. An item is shipped only after direct behavioral evidence; external state and unsafe L3 rewrites remain explicit deferrals.

**Completion:** All 14/14 items shipped with direct evidence. The cumulative audit is 37/37 shipped, the regenerated innovation pack is 6/6 shipped, and executable exhaustion reports zero pending unblocked work. Live-only launch evidence remains red by design.

## Wave A — Canonical truth and runtime safety

1. `quorum-attested-match-notary` — strict active-IP majority, complete canonical roster, tamper-evident result certificate, one progression/archive input, idempotency.
2. `game-create-authorization-capacity` — exact actor/admin authorization, collision rejection, actor/IP quotas, worker capacity, stable outcomes.
3. `state-scope-ledger-database-readiness` — explicit database state machine and compiled durability/readiness ledger.
4. `bounded-sse-transport` — subscriber ceilings, bounded queues, backpressure eviction, counters, runtime posture.
5. `canonical-ai-input-cache` — authenticated certificate-grounded inputs, full digest cache keys, bounded retention/deadlines, validated outputs.

## Wave B — Product liveness, privacy, and performance

6. `feature-liveness-command-center` — responsive Command Center, real meta-feature mounts, unlock feedback, route/API/mount/journey liveness graph.
7. `privacy-safe-notification-boundary` — no identity/chat logs, bounded Discord payloads, mentions disabled, deadline/concurrency posture.
8. `vault-status-delta-projection` — cadence-bounded invalidation, digest suppression, immediate critical events, build/publish metrics.
9. `route-manifest-composition-root` — typed route policy manifest and semantic coverage for high-risk Worker routes.

## Wave C — Release and automation truth

10. `release-evidence-completeness` — fail-closed canonical gates, provenance/freshness, `/_health`, footer manifest/probe, no fabricated external proof.
11. `task-parser-and-brief-coherence` — one parser plus explicit parse/source/session/hash freshness states.
12. `spawn-hygiene-closure` — safe-spawn coverage across dynamic imports, TypeScript, tests, and performance runners; remove dead renderer branch.
13. `automation-aware-pr-and-release-workflows` — trusted automation contract and dormant upstream release-path removal.
14. `deploy-contract-hardening` — one dry-runnable entrypoint, immutable digests, bounded cleanup, health/staging inputs, shell tests.

## Expansion gate

After all 14 items have direct evidence, regenerate the Unified Genius List, require zero pending items, generate the second-order innovation pack, implement every locally valid candidate, and rerun exhaustion before `/closeout`.
